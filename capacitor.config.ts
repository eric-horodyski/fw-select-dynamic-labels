import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw-select-dynamic-labels',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
